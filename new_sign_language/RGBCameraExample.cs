using UnityEngine;

// Local camera texture bridge used by Test.cs.
// It preserves the expected texture fields without changing that logic.
public class RGBCameraExample : MonoBehaviour
{
    public Texture y_texture;
    public Texture u_texture;
    public Texture v_texture;
}
